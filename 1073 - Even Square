#include<stdio.h>
#include<math.h>
int main(){
    int n;
    int result;
    scanf("%d", &n);
    for(int i = 1; i <= n; i++){
        if(i % 2 == 0){
            result = i*i;
            printf("%d^%d = %d\n", i,2,result);
        }
        
    }
}
